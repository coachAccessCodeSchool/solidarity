{% extends 'base.html.twig' %}{% block title 'Créer un produit' %}
{% block body %}
    <div class="mt-5">
        <div class="container col-md-6 m-auto">
            {{ form_start(form) }}
                {{ form_row(form.name) }}
                {{ form_row(form.information) }}
                <button type="submit" class="btn btn-primary">Créer</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}